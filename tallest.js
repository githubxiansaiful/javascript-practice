const height = [10, 23, 32, 43, 52, 29, 60, 78, 90];

function getMaxHeight(numbers) {
    for(const num of numbers) {
        console.log(num);
    }
}
const maxHeight = getMaxHeight(height);
console.log("The max value is: ", maxHeight);